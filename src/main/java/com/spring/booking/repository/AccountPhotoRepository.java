package com.spring.booking.repository;

import com.spring.booking.models.AccountPhoto;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AccountPhotoRepository extends JpaRepository<AccountPhoto, Integer> {
}
